using UnityEngine;
using System.Collections;

namespace SwapDrop.Models {
  /// <summary>
  /// Possible colors of gem to spawn, one per player.
  /// </summary>
  public enum GemType {
    BLUE,
    RED
  }
}